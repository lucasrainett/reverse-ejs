# Dependencies
node_modules/

# Build output
dist/

# Test coverage
coverage/

# Playwright
playwright-report/
test-results/
.playwright/

# Performance results — only the CI runner is allowed to commit this file,
# so local `pnpm perf` runs never accidentally land in git. CI bypasses
# this rule with `git add -f perf/results.json`.
perf/results.json

# Test fixtures (downloaded) and rendered outputs (generated)
tests/fixtures/
tests/rendered/

# IDE
.idea/

# Claude
/.claude/
# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Lock files (keep pnpm-lock.yaml)
package-lock.json
yarn.lock
